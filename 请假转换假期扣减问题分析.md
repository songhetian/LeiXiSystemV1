# 请假转换假期扣减问题分析

## 📋 问题描述

**问题**：请假申请使用转换假期抵扣后，审核通过后，剩余的转换假期没有减少。

**影响**：用户可以重复使用同一份转换假期，导致假期余额不准确。

---

## 🔍 代码逻辑分析

### 1. 数据库表结构

#### vacation_conversions（假期转换记录表）
```sql
CREATE TABLE `vacation_conversions` (
  `id` int NOT NULL AUTO_INCREMENT COMMENT '转换记录ID',
